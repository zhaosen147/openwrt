#!/bin/sh
#
# Copyright (C) 2015 OpenWrt.org
#

. /lib/functions/uci-defaults.sh

board_config_update

board=$(board_name)
boardname="${board##*,}"

case "$board" in
xiaomi,ax3600)
	ucidef_set_led_netdev "wan" "WAN" "${boardname}:blue:network" "pppoe-wan"
	;;
esac

board_config_flush

exit 0
